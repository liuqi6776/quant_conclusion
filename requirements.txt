# Core quantitative finance and data processing dependencies
pandas>=1.3.0
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.4.0
pytest>=7.0.0
pyyaml>=5.4.0

# Optional / recommended Parquet storage engines (gracefully falls back to CSV if missing)
pyarrow>=8.0.0
fastparquet>=0.8.0
